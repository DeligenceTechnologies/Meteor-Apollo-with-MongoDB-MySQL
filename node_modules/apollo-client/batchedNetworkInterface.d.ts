/// <reference types="isomorphic-fetch" />
/// <reference types="typed-graphql" />
import { HTTPFetchNetworkInterface, RequestAndOptions, Request, BatchedNetworkInterface } from './networkInterface';
import { GraphQLResult } from 'graphql';
import 'whatwg-fetch';
export declare class HTTPBatchedNetworkInterface extends HTTPFetchNetworkInterface implements BatchedNetworkInterface {
    constructor(uri: string, opts: RequestInit);
    batchedFetchFromRemoteEndpoint(requestsAndOptions: RequestAndOptions[]): Promise<IResponse>;
    batchQuery(requests: Request[]): Promise<GraphQLResult[]>;
}
